import React, { useEffect, useRef, useState } from 'react';import mermaid from 'mermaid';import { SSEDiagramEvent } from '../../types';import { GitFork, Code2, AlertTriangle, Check, Copy, Maximize2, Minimize2 } from 'lucide-react';interface DiagramRendererProps {  diagram: SSEDiagramEvent;}mermaid.initialize({  startOnLoad: false,  theme: 'dark',  securityLevel: 'strict',  fontFamily: 'JetBrains Mono, monospace',  themeVariables: {    darkMode: true,    background: '#170b07',    primaryColor: '#2a1d17',    primaryTextColor: '#f6ddd4',    primaryBorderColor: '#ffb595',    lineColor: '#ffb595',    secondaryColor: '#352721',    tertiaryColor: '#251913',    noteBkgColor: '#251913',    noteTextColor: '#e0c0b2',    noteBorderColor: '#a88a7e',    activationBorderColor: '#ee671c',    activationBkgColor: '#352721',    actorBkg: '#2a1d17',    actorBorder: '#ffb595',    actorTextColor: '#f6ddd4',    actorLineColor: '#a88a7e',    signalColor: '#f6ddd4',    signalTextColor: '#f6ddd4',    labelBoxBkgColor: '#352721',    labelBoxBorderColor: '#a88a7e',    labelTextColor: '#f6ddd4',    loopTextColor: '#e0c0b2',    fillType0: '#2a1d17',    fillType1: '#352721',    fillType2: '#40312b',    fillType3: '#a88a7e',    fillType4: '#e0c0b2',    fillType5: '#ffb595',    fillType6: '#ee671c',    fillType7: '#bbcbb9',    classText: '#f6ddd4',    titleColor: '#ffb595',    edgeLabelBackground: '#170b07',    clusterBkg: '#251913',    clusterBorder: '#594238',  },});export const DiagramRenderer: React.FC<DiagramRendererProps> = ({ diagram }) => {  const containerRef = useRef<HTMLDivElement>(null);  const [svgContent, setSvgContent] = useState<string>('');  const [error, setError] = useState<string | null>(null);  const [showRaw, setShowRaw] = useState<boolean>(false);  const [copied, setCopied] = useState<boolean>(false);  const [fullscreen, setFullscreen] = useState<boolean>(false);  useEffect(() => {    let isMounted = true;    const renderDiagram = async () => {      if (!diagram || !diagram.mermaid) {        setError('Empty diagram definition.');        return;      }      const id = `mermaid_${Math.random().toString(36).substring(2, 9)}`;      try {        const { svg } = await mermaid.render(id, diagram.mermaid);        if (isMounted) {          setSvgContent(svg);          setError(null);        }      } catch (err: any) {        console.warn('Mermaid render error:', err);        if (isMounted) {          setError(err?.message || 'Failed to render diagram syntax.');        }      }    };    renderDiagram();    return () => {      isMounted = false;    };  }, [diagram]);  const handleCopyCode = () => {    navigator.clipboard.writeText(diagram.mermaid);    setCopied(true);    setTimeout(() => setCopied(false), 2000);  };  const typeLabel = (diagram.diagram_type || 'diagram').toUpperCase();  return (    <div      className={`my-3 p-4 rounded-lg bg-surface-container-lowest border border-outline-variant shadow-sm ${        fullscreen ? 'fixed inset-4 z-50 overflow-auto' : ''      }`}    >      <div className="flex items-center justify-between mb-3 pb-2 border-b border-outline-variant">        <div className="flex items-center gap-2 text-primary">          <GitFork className="w-4 h-4" />          <span className="font-label-md text-label-md uppercase tracking-wider font-semibold">            {diagram.title || `${typeLabel} DIAGRAM`}         </span>       </div>        <div className="flex items-center gap-1.5">          <button            onClick={() => setShowRaw(!showRaw)}            className="flex items-center gap-1 px-2 py-1 rounded bg-surface-container border border-outline-variant text-on-surface-variant hover:text-primary hover:border-primary transition-colors font-label-caps text-label-caps"          >            <Code2 className="w-3 h-3" />            <span>{showRaw ? 'RENDER' : 'CODE'</span>         </button>          <button            onClick={handleCopyCode}            className="p-1.5 rounded bg-surface-container border border-outline-variant text-on-surface-variant hover:text-primary hover:border-primary transition-colors"            title="Copy Mermaid code"          >            {copied ? <Check className="w-3.5 h-3.5 text-secondary" /> : <Copy className="w-3.5 h-3.5" />}         </button>          <button            onClick={() => setFullscreen(!fullscreen)}            className="p-1.5 rounded bg-surface-container border border-outline-variant text-on-surface-variant hover:text-primary hover:border-primary transition-colors"            title={fullscreen ? 'Exit fullscreen' : 'View fullscreen'}          >            {fullscreen ? <Minimize2 className="w-3.5 h-3.5" /> : <Maximize2 className="w-3.5 h-3.5" />}         </button>       </div>     </div>      {showRaw ? (        <div className="p-3 rounded-lg bg-surface-container-lowest font-mono text-label-sm text-secondary overflow-x-auto border border-outline-variant">          <pre>{diagram.mermaid</pre>       </div>      ) : error ? (        <div className="p-3 rounded-lg bg-surface-container-lowest border border-outline-variant text-label-sm">          <div className="flex items-center gap-2 text-primary font-semibold mb-1">            <AlertTriangle className="w-4 h-4" />            <span>DIAGRAM SYNTAX FALLBACK</span>         </div>          <p className="text-on-surface-variant mb-2 font-body-sm">            Unable to render visual SVG; displaying raw source.         </p>          <pre className="font-mono text-secondary overflow-x-auto p-2 bg-surface-container rounded">            {diagram.mermaid}         </pre>       </div>      ) : (        <div          ref={containerRef}          className="svg-container flex justify-center items-center overflow-x-auto p-3 bg-surface-container-lowest rounded-lg border border-outline-variant min-h-[180px]"          dangerouslySetInnerHTML={{ __html: svgContent }}        />      )}   </div>  );};