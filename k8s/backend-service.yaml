apiVersion: v1
kind: Service
metadata:
  name: dataflow-backend
  namespace: dataflow
spec:
  selector:
    app: dataflow-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
